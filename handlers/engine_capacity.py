from telegram import Update
from telegram.ext import ContextTypes

from handlers.handlers import show_specific_keyboard_to_change_order
from keyboards.keyboards import Keyboard
from routes.routes import StartEndRoutes


async def _10(update: Update, context: ContextTypes.DEFAULT_TYPE) -> StartEndRoutes:
    return await show_specific_keyboard_to_change_order(update, context, "engine_capacity", "1.0",
                                                        Keyboard.ENGINE_CAPACITY_KEYBOARD)


async def _11(update: Update, context: ContextTypes.DEFAULT_TYPE) -> StartEndRoutes:
    return await show_specific_keyboard_to_change_order(update, context, "engine_capacity", "1.1",
                                                        Keyboard.ENGINE_CAPACITY_KEYBOARD)


async def _12(update: Update, context: ContextTypes.DEFAULT_TYPE) -> StartEndRoutes:
    return await show_specific_keyboard_to_change_order(update, context, "engine_capacity", "1.2",
                                                        Keyboard.ENGINE_CAPACITY_KEYBOARD)


async def _13(update: Update, context: ContextTypes.DEFAULT_TYPE) -> StartEndRoutes:
    return await show_specific_keyboard_to_change_order(update, context, "engine_capacity", "1.3",
                                                        Keyboard.ENGINE_CAPACITY_KEYBOARD)


async def _14(update: Update, context: ContextTypes.DEFAULT_TYPE) -> StartEndRoutes:
    return await show_specific_keyboard_to_change_order(update, context, "engine_capacity", "1.4",
                                                        Keyboard.ENGINE_CAPACITY_KEYBOARD)


async def _15(update: Update, context: ContextTypes.DEFAULT_TYPE) -> StartEndRoutes:
    return await show_specific_keyboard_to_change_order(update, context, "engine_capacity", "1.5",
                                                        Keyboard.ENGINE_CAPACITY_KEYBOARD)


async def _16(update: Update, context: ContextTypes.DEFAULT_TYPE) -> StartEndRoutes:
    return await show_specific_keyboard_to_change_order(update, context, "engine_capacity", "1.6",
                                                        Keyboard.ENGINE_CAPACITY_KEYBOARD)


async def _17(update: Update, context: ContextTypes.DEFAULT_TYPE) -> StartEndRoutes:
    return await show_specific_keyboard_to_change_order(update, context, "engine_capacity", "1.7",
                                                        Keyboard.ENGINE_CAPACITY_KEYBOARD)


async def _18(update: Update, context: ContextTypes.DEFAULT_TYPE) -> StartEndRoutes:
    return await show_specific_keyboard_to_change_order(update, context, "engine_capacity", "1.8",
                                                        Keyboard.ENGINE_CAPACITY_KEYBOARD)


async def _19(update: Update, context: ContextTypes.DEFAULT_TYPE) -> StartEndRoutes:
    return await show_specific_keyboard_to_change_order(update, context, "engine_capacity", "1.9",
                                                        Keyboard.ENGINE_CAPACITY_KEYBOARD)


async def _20(update: Update, context: ContextTypes.DEFAULT_TYPE) -> StartEndRoutes:
    return await show_specific_keyboard_to_change_order(update, context, "engine_capacity", "2.0",
                                                        Keyboard.ENGINE_CAPACITY_KEYBOARD)


async def _21(update: Update, context: ContextTypes.DEFAULT_TYPE) -> StartEndRoutes:
    return await show_specific_keyboard_to_change_order(update, context, "engine_capacity", "2.1",
                                                        Keyboard.ENGINE_CAPACITY_KEYBOARD)


async def _22(update: Update, context: ContextTypes.DEFAULT_TYPE) -> StartEndRoutes:
    return await show_specific_keyboard_to_change_order(update, context, "engine_capacity", "2.2",
                                                        Keyboard.ENGINE_CAPACITY_KEYBOARD)


async def _23(update: Update, context: ContextTypes.DEFAULT_TYPE) -> StartEndRoutes:
    return await show_specific_keyboard_to_change_order(update, context, "engine_capacity", "2.3",
                                                        Keyboard.ENGINE_CAPACITY_KEYBOARD)


async def _24(update: Update, context: ContextTypes.DEFAULT_TYPE) -> StartEndRoutes:
    return await show_specific_keyboard_to_change_order(update, context, "engine_capacity", "2.4",
                                                        Keyboard.ENGINE_CAPACITY_KEYBOARD)


async def _25(update: Update, context: ContextTypes.DEFAULT_TYPE) -> StartEndRoutes:
    return await show_specific_keyboard_to_change_order(update, context, "engine_capacity", "2.5",
                                                        Keyboard.ENGINE_CAPACITY_KEYBOARD)


async def _26(update: Update, context: ContextTypes.DEFAULT_TYPE) -> StartEndRoutes:
    return await show_specific_keyboard_to_change_order(update, context, "engine_capacity", "2.6",
                                                        Keyboard.ENGINE_CAPACITY_KEYBOARD)


async def _27(update: Update, context: ContextTypes.DEFAULT_TYPE) -> StartEndRoutes:
    return await show_specific_keyboard_to_change_order(update, context, "engine_capacity", "2.7",
                                                        Keyboard.ENGINE_CAPACITY_KEYBOARD)


async def _28(update: Update, context: ContextTypes.DEFAULT_TYPE) -> StartEndRoutes:
    return await show_specific_keyboard_to_change_order(update, context, "engine_capacity", "2.8",
                                                        Keyboard.ENGINE_CAPACITY_KEYBOARD)


async def _29(update: Update, context: ContextTypes.DEFAULT_TYPE) -> StartEndRoutes:
    return await show_specific_keyboard_to_change_order(update, context, "engine_capacity", "2.9",
                                                        Keyboard.ENGINE_CAPACITY_KEYBOARD)


async def _30(update: Update, context: ContextTypes.DEFAULT_TYPE) -> StartEndRoutes:
    return await show_specific_keyboard_to_change_order(update, context, "engine_capacity", "3.0",
                                                        Keyboard.ENGINE_CAPACITY_KEYBOARD)


async def _31(update: Update, context: ContextTypes.DEFAULT_TYPE) -> StartEndRoutes:
    return await show_specific_keyboard_to_change_order(update, context, "engine_capacity", "3.1",
                                                        Keyboard.ENGINE_CAPACITY_KEYBOARD)


async def _32(update: Update, context: ContextTypes.DEFAULT_TYPE) -> StartEndRoutes:
    return await show_specific_keyboard_to_change_order(update, context, "engine_capacity", "3.2",
                                                        Keyboard.ENGINE_CAPACITY_KEYBOARD)


async def _33(update: Update, context: ContextTypes.DEFAULT_TYPE) -> StartEndRoutes:
    return await show_specific_keyboard_to_change_order(update, context, "engine_capacity", "3.3",
                                                        Keyboard.ENGINE_CAPACITY_KEYBOARD)


async def _34(update: Update, context: ContextTypes.DEFAULT_TYPE) -> StartEndRoutes:
    return await show_specific_keyboard_to_change_order(update, context, "engine_capacity", "3.4",
                                                        Keyboard.ENGINE_CAPACITY_KEYBOARD)


async def _35(update: Update, context: ContextTypes.DEFAULT_TYPE) -> StartEndRoutes:
    return await show_specific_keyboard_to_change_order(update, context, "engine_capacity", "3.5",
                                                        Keyboard.ENGINE_CAPACITY_KEYBOARD)


async def _36(update: Update, context: ContextTypes.DEFAULT_TYPE) -> StartEndRoutes:
    return await show_specific_keyboard_to_change_order(update, context, "engine_capacity", "3.6",
                                                        Keyboard.ENGINE_CAPACITY_KEYBOARD)


async def _37(update: Update, context: ContextTypes.DEFAULT_TYPE) -> StartEndRoutes:
    return await show_specific_keyboard_to_change_order(update, context, "engine_capacity", "3.7",
                                                        Keyboard.ENGINE_CAPACITY_KEYBOARD)


async def _38(update: Update, context: ContextTypes.DEFAULT_TYPE) -> StartEndRoutes:
    return await show_specific_keyboard_to_change_order(update, context, "engine_capacity", "3.8",
                                                        Keyboard.ENGINE_CAPACITY_KEYBOARD)


async def _39(update: Update, context: ContextTypes.DEFAULT_TYPE) -> StartEndRoutes:
    return await show_specific_keyboard_to_change_order(update, context, "engine_capacity", "3.9",
                                                        Keyboard.ENGINE_CAPACITY_KEYBOARD)


async def _40(update: Update, context: ContextTypes.DEFAULT_TYPE) -> StartEndRoutes:
    return await show_specific_keyboard_to_change_order(update, context, "engine_capacity", "4.0",
                                                        Keyboard.ENGINE_CAPACITY_KEYBOARD)


async def _41(update: Update, context: ContextTypes.DEFAULT_TYPE) -> StartEndRoutes:
    return await show_specific_keyboard_to_change_order(update, context, "engine_capacity", "4.1",
                                                        Keyboard.ENGINE_CAPACITY_KEYBOARD)


async def _42(update: Update, context: ContextTypes.DEFAULT_TYPE) -> StartEndRoutes:
    return await show_specific_keyboard_to_change_order(update, context, "engine_capacity", "4.2",
                                                        Keyboard.ENGINE_CAPACITY_KEYBOARD)


async def _43(update: Update, context: ContextTypes.DEFAULT_TYPE) -> StartEndRoutes:
    return await show_specific_keyboard_to_change_order(update, context, "engine_capacity", "4.3",
                                                        Keyboard.ENGINE_CAPACITY_KEYBOARD)


async def _44(update: Update, context: ContextTypes.DEFAULT_TYPE) -> StartEndRoutes:
    return await show_specific_keyboard_to_change_order(update, context, "engine_capacity", "4.4",
                                                        Keyboard.ENGINE_CAPACITY_KEYBOARD)


async def _45(update: Update, context: ContextTypes.DEFAULT_TYPE) -> StartEndRoutes:
    return await show_specific_keyboard_to_change_order(update, context, "engine_capacity", "4.5",
                                                        Keyboard.ENGINE_CAPACITY_KEYBOARD)


async def _46(update: Update, context: ContextTypes.DEFAULT_TYPE) -> StartEndRoutes:
    return await show_specific_keyboard_to_change_order(update, context, "engine_capacity", "4.6",
                                                        Keyboard.ENGINE_CAPACITY_KEYBOARD)


async def _47(update: Update, context: ContextTypes.DEFAULT_TYPE) -> StartEndRoutes:
    return await show_specific_keyboard_to_change_order(update, context, "engine_capacity", "4.7",
                                                        Keyboard.ENGINE_CAPACITY_KEYBOARD)


async def _48(update: Update, context: ContextTypes.DEFAULT_TYPE) -> StartEndRoutes:
    return await show_specific_keyboard_to_change_order(update, context, "engine_capacity", "4.8",
                                                        Keyboard.ENGINE_CAPACITY_KEYBOARD)


async def _49(update: Update, context: ContextTypes.DEFAULT_TYPE) -> StartEndRoutes:
    return await show_specific_keyboard_to_change_order(update, context, "engine_capacity", "4.9",
                                                        Keyboard.ENGINE_CAPACITY_KEYBOARD)


async def _50(update: Update, context: ContextTypes.DEFAULT_TYPE) -> StartEndRoutes:
    return await show_specific_keyboard_to_change_order(update, context, "engine_capacity", "5.0",
                                                        Keyboard.ENGINE_CAPACITY_KEYBOARD)


async def _51(update: Update, context: ContextTypes.DEFAULT_TYPE) -> StartEndRoutes:
    return await show_specific_keyboard_to_change_order(update, context, "engine_capacity", "5.1",
                                                        Keyboard.ENGINE_CAPACITY_KEYBOARD)


async def _52(update: Update, context: ContextTypes.DEFAULT_TYPE) -> StartEndRoutes:
    return await show_specific_keyboard_to_change_order(update, context, "engine_capacity", "5.2",
                                                        Keyboard.ENGINE_CAPACITY_KEYBOARD)


async def _53(update: Update, context: ContextTypes.DEFAULT_TYPE) -> StartEndRoutes:
    return await show_specific_keyboard_to_change_order(update, context, "engine_capacity", "5.3",
                                                        Keyboard.ENGINE_CAPACITY_KEYBOARD)


async def _54(update: Update, context: ContextTypes.DEFAULT_TYPE) -> StartEndRoutes:
    return await show_specific_keyboard_to_change_order(update, context, "engine_capacity", "5.4",
                                                        Keyboard.ENGINE_CAPACITY_KEYBOARD)


async def _55(update: Update, context: ContextTypes.DEFAULT_TYPE) -> StartEndRoutes:
    return await show_specific_keyboard_to_change_order(update, context, "engine_capacity", "5.5",
                                                        Keyboard.ENGINE_CAPACITY_KEYBOARD)


async def _56(update: Update, context: ContextTypes.DEFAULT_TYPE) -> StartEndRoutes:
    return await show_specific_keyboard_to_change_order(update, context, "engine_capacity", "5.6",
                                                        Keyboard.ENGINE_CAPACITY_KEYBOARD)


async def _57(update: Update, context: ContextTypes.DEFAULT_TYPE) -> StartEndRoutes:
    return await show_specific_keyboard_to_change_order(update, context, "engine_capacity", "5.7",
                                                        Keyboard.ENGINE_CAPACITY_KEYBOARD)


async def _58(update: Update, context: ContextTypes.DEFAULT_TYPE) -> StartEndRoutes:
    return await show_specific_keyboard_to_change_order(update, context, "engine_capacity", "5.8",
                                                        Keyboard.ENGINE_CAPACITY_KEYBOARD)


async def _59(update: Update, context: ContextTypes.DEFAULT_TYPE) -> StartEndRoutes:
    return await show_specific_keyboard_to_change_order(update, context, "engine_capacity", "5.9",
                                                        Keyboard.ENGINE_CAPACITY_KEYBOARD)
